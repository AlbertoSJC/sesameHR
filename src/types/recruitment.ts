export interface InputProps {
  label?: string;
  placeholder?: string;
  classes?: string;
  type?: string;
  id?: string;
}

export enum RecruitmentTabs {
  Vacancies = 'Vacantes',
  Candidates = 'Candidatos',
}
